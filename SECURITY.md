# 安全政策

## 报告漏洞

请通过 GitHub Security Advisories 私下报告漏洞，不要在公开 issue 中提供可利用细节、密钥、
设备令牌、儿童数据或服务器地址。维护者会尽快确认影响范围和修复计划。

## 部署要求

- `.env` 必须保持 `0600`，API Key 只保存在后端。
- 家庭 HTTP 服务只能用于可信局域网；公网必须使用 HTTPS 或可信 VPN。
- 不要开放数据库端口，不要转发 TCP 8000 到公网。
- 使用一次性绑定码，丢失设备后撤销其令牌。
- APK 只从项目 Releases 或你自己的可信构建获取，并核对 SHA-256 与签名指纹。
- 定期更新 Docker 基础镜像、Python 依赖和 Android 应用。

本项目不承诺适用于紧急求助、医疗诊断或无人监管的高风险场景。
